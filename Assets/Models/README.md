# Models

ここに `.glb` ファイルを配置すると、Unity (glTFast) が自動的にインポートし、Prefabとして使えるオブジェクトが生成されます。

生成されたオブジェクトを `Assets/Scenes/ARScene.unity` の `XR Origin` にアタッチされている `Auto Place On Plane` コンポーネントの `Model Prefab` にドラッグ＆ドロップして割り当ててください。
